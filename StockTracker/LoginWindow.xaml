<Window x:Class="StockTracker.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="群益股票監測系統 - 登入" Height="311" Width="420" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource AppBackgroundBrush}" WindowStyle="None">
    <Grid Margin="24" Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="4" Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="1" MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown">
            <DockPanel LastChildFill="True" Margin="8,0">
                <Button Content="✕" Width="36" Height="24" DockPanel.Dock="Right" Click="CloseButton_OnClick"/>
                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="群益 API 登入"/>
            </DockPanel>
        </Border>

        <TextBlock Grid.Row="1" Text="群益 API 登入" FontSize="24" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="3">
            <TextBlock Text="帳號" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Account, UpdateSourceTrigger=PropertyChanged}" Height="20"/>
        </StackPanel>

        <StackPanel Grid.Row="5">
            <TextBlock Text="密碼" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordInput" Height="20" PasswordChanged="PasswordBox_OnPasswordChanged"/>
        </StackPanel>

        <CheckBox Grid.Row="7" x:Name="RememberCredentialsCheckBox" Content="記住帳號密碼" VerticalAlignment="Center" Foreground="#FFFFF9F9"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" VerticalAlignment="Center">
            <Button Content="登入群益 API" Name="btnLogin" Width="140" Height="34" Click="btnLogin_Click" />
            <TextBlock Text="{Binding StatusMessage}" Margin="16,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource AccentBrush}"/>
        </StackPanel>
    </Grid>
</Window>
